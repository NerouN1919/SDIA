apiVersion: traefik.io/v1alpha1
kind: TraefikService
metadata:
  name: payment-system-trafficsplit
  namespace: default
spec:
  weighted:
    services:
      - name: wallet-service-stable
        port: 80
        weight: 100
      - name: wallet-service-canary
        port: 80
        weight: 0